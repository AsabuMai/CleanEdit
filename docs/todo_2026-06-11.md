# Execution Queue 2026-06-11 (Post T5/T6/Core-5 Rescope)

## Current Override 2026-06-12

Phase2 paper evidence is now locked for writing. The selected-run metric refresh
finished on `a100-01` via Slurm job 719870: 33/33 repaired/formal DeCE-RF
visual selections were re-evaluated, merged into `phase2_internal_bg_metrics.csv`,
and the selected-run registry now reports 540/540 metric-aligned rows.

Use `CURRENT_PHASE2_STATUS_2026-06-11.md` and `PHASE2_LOCK_2026-06-11.md` as
the active status files. The remaining work is manuscript writing plus optional
human ratings if the paper wants human visual-audit claims.

## Session-End Status (2026-06-11 ~01:30 JST)

- **P0-1 DONE** (commit b8b68e1): cable_knit eval mask frozen + registered;
  E1 Core-5 matrix 60/60; `e1_core5_strict_metrics.csv` carries all Table 1
  columns. Headline: DeCE-RF 9/9 relation-style success checks on T1/T2/T3, outside L1 0.0393, SSIM 0.709,
  CLIP edit +0.048; generic support 1/9; direct target 0/9; T4/T5 use recolor/material-specific metrics and visual gates rather than this boolean, worst preserve.
- **P0-2 + P0-3 DONE**: Slurm job **715378**
  (`run_p0_e2_finish_20260611.sbatch`) completed on `a100-01`; stage 1 filled
  the E2 T1-T4 baseline CLIP+DINO/edit-score metrics, and stage 2 computed the
  missing Phase 2 direct_target/generic rows. Table previews were generated by
  `scripts/build_paper_table_previews.py`.
- Current correction resolved: the first T5 full run used old center-panel
  `pillow_same_color_linen_panel` and `pillow_same_color_terry_panel` rows.
  Those rows are diagnostic only. The formal T5-1-style full-pillow set is now
  complete for `pillow_same_color_cable_knit`,
  `pillow_same_color_cable_knit_grey`, and
  `pillow_same_color_cable_knit_armchair`; outputs were filled by job 717149
  and corrected metrics by job 717295.
- After that: P1 in order (#4 E4 feedback regime wording already demoted,
  #5 direct-target same-M_edit gate done / FlowEdit wrapper blocked, #6 mask
  sensitivity, #7 efficiency table).
- Gotcha logged today: never `git add` the eval-mask PNGs (gitignored by
  convention; only eval_masks_metadata.json is tracked) — a blocked add aborts
  the whole command chain.

Consolidated from the 2026-06-10/11 session: E1-E5 consolidation findings, T5
cable-knit rebuild, T6 -> E5 removal probe, Core-5 rescope, baseline layering
A-E, EEdit presentation pass, and the task-specific success-check validation.
Priorities: P0 blocks the main tables; P1 is reviewer defense; P2 strengthens
the submission; P3 is housekeeping.

## P0 - Blocks Main-Table Data (run immediately; jobs only, no unknowns)

1. **cable_knit fixed eval mask + E1 Core-5 matrix.**
   5 tasks x 4 methods x 3 seeds = 60 runs, then a full metric pass with the
   grouped columns (incl. `--success-spec` task-specific checks). DONE
   2026-06-11; this is the current Table 1 source.
2. **E2 finishing. DONE 2026-06-11.**
   Re-ran the E2 T1-T4 baseline metrics with CLIP (cache fixed 2026-06-10) to
   fill the empty edit-score column; join `adaptive_full_generic_support` into
   Table 2a from the E1 cache (no new generation). Completed by job 715378; Table 2a preview should use the new summary file.
3. **Phase 2 expansion gap-fill. DONE for T1-T5 2026-06-11.**
   Computed the missing direct_target / generic-support fixed-mask metric rows
   for the 12 Phase 2 T1-T4 tasks (outputs exist). The formal T5 set is
   `pillow_same_color_cable_knit`, `pillow_same_color_cable_knit_grey`, and
   `pillow_same_color_cable_knit_armchair`; grey/armchair are no longer
   optional probes if T5 enters final Table 2. Corrected T5 coverage is 45/45
   internal metric rows and 54/54 baseline metric rows.

## P1 - Reviewer Defense (start after P0 pipeline is in motion)

4. **E4: find a regime where feedback beats Fixed DeCE** (support perturbation
   / stronger stress per the design). Biggest open claim risk: current
   aggregates show fixed ~= feedback. If no regime is found, formally demote
   the manuscript claim to "tradeoff stabilizer" (design wording already
   allows this).
5. **E2.4 redo bundle: direct-target gate DONE; FlowEdit wrapper BLOCKED.**
   The aligned `direct_target_medit_gate` inference-time same fixed-M_edit row
   is complete (job 716750/716794; 36/36 outputs and metrics). FlowEdit remains
   diagnostic post-hoc only because no verified mask-aware inference wrapper is
   available in this project. Do not promote old misaligned/blend rows into a
   main table.
6. **Mask sensitivity sweep:** recompute rankings under eroded/base/dilated
   evaluation masks. The main text already promises "ranking is stable";
   metrics-only, light.
7. **Efficiency context table:** NFE / forward passes / inversion use /
   resolution / runtime / peak GPU memory for all methods (runtime+memory
   already in every metadata.json). Pre-empts "gain bought with compute".

## P2 - Submission Strengthening

8. **Blind internal audit execution:** 3 raters, randomized hidden methods,
   1-5 rubric (protocol in the design doc). Run once Table 1 outputs are
   frozen; needs humans.
9. **Optional baseline rows:** register + smoke FIA-Edit-SD3 (unregistered
   native-SD3 repo found in _baselines/src; potential Table 2a row); OT-RF
   strict adapter (C-layer priority 2; env+smoke already ok).
10. **Core-5 writing pass over manuscript.md / tables.md / figures.md /
    limitations.md** — after Table 1 numbers exist, to avoid a second rewrite.

## P3 - Housekeeping

11. Refresh stale baseline registries (`e2_baseline_runnable_validation.csv`
    and e2_status_lock rows say OT-RF/DVRF "planned_not_downloaded"; both are
    downloaded with built envs and passing smoke).
12. Design-doc backfill: E4 status section still says 2026-06-04 (T1-T4 E4
    completed 2026-06-09); reflect the Phase 2 T1-T4 144/144 expansion state.
13. Switch the GitHub default branch from codex/rf-h-edit-cleanup to main
    (web settings, one minute).

## Suggested Cadence

Pipeline P0: submit the #1 GPU matrix first (longest), run #2 metrics and
P3 #11/#12 while it generates, then one combined metric pass closes #3.
P1 #6/#7 are metrics-only and can interleave; #4 and #5 are the two real
design-work items and get dedicated focus after P0.

## Standing Notes

- Strict E1 set is Core-5 (T1-T5); removal is the E5 boundary probe with
  removal-aware metrics. Revised Core-5 is unrelated to the archived 2026-05
  Core-5.
- Presence scores from GroundingDINO alone are unreliable; task success is
  presence AND relation (validated against the human audit, 2026-06-11).
- Never `git add` whole experiment directories without checking for archives;
  push via the local relay (server cannot reach GitHub).
