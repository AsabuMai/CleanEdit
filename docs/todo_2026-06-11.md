# Execution Queue 2026-06-29

This queue supersedes the old 2026-06-11 Phase2 execution queue. Phase2 is now
historical/internal diagnostic evidence; the current paper scope is
FlowEdit-135.

## Current Status

- FlowEdit-135 main-table scope is complete after excluding SAM-Flow from the
  main peer-reviewed baseline set: 135 tasks x 12 methods x seed 10 = 1620
  metric rows.
- No-final-restore Ours rows are complete: Ours-SD3 and Ours-FLUX = 270/270
  rows, completed on `a100-01` by Slurm job 769100.
- Peer-reviewed baseline rows are inherited from the 2026-06-21 FlowEdit-135
  metric pass.
- Current main-table source:
  `experiments/norestore_tradeoff/summary_main_no_samflow.csv`.
- Current no-final-restore Ours metrics:
  `experiments/norestore_metrics/metrics.csv`.
- Current paper guidance lives in `paper/README.md`, `paper/results.md`,
  `paper/tables.md`, `paper/figures.md`, and `paper/limitations.md`.

## P0 - Paper Consistency

1. Keep root `README.md`, `PROJECT_MAP.md`, and `docs/README.md` aligned with
   the FlowEdit-135 scope.
2. Do not mix old Phase2 table numbers into the FlowEdit-135 main result.
3. State the claim as preservation-first: strongest non-edit-region
   preservation while remaining on the edit-preservation Pareto frontier.
4. Do not claim strongest edit amplitude, broad image-editing SOTA, robust text
   replacement, or human-study evidence.

## P1 - Figures And Tables Before Submission

1. Refresh or filter the main trade-off figures so `sam_flow_sd3` and
   `sam_flow_flux` are not shown in main-paper plots.
2. Rebuild a no-final-restore family summary before using family-rank language
   in the main text.
3. Build a smaller qualitative comparison crop from the no-final-restore
   outputs:
   `source | fixed eval region | Ours-SD3 | Ours-FLUX | FlowEdit-SD3 |
   SplitFlow-SD3 | OT-RF enhanced`.
4. Use T3 text-like hard cases as limitations, not as positive-only examples.

## P2 - Optional Reviewer Context

1. If SAM-Flow is discussed, label it as concurrent arXiv context or appendix
   material, not a main peer-reviewed baseline.
2. If human ratings are desired, run real human annotation. The older Phase2
   proxy blind-audit sheets are internal prechecks only.
3. If runtime/compute questions become central, report NFE, forward passes,
   inversion use, resolution, runtime, and peak GPU memory from metadata.

## P3 - Git And Workspace Hygiene

1. Review `git status -sb` before staging. The worktree contains many generated
   experiment outputs and sbatch files.
2. Stage files intentionally rather than adding whole experiment directories.
3. Keep zero-byte shell residuals out of the root directory.
4. Keep heavy install/model/GPU/Torch/diffusers work off the master node.

## Compute Boundary

Master node is only for light file work, queue checks, aggregation, and job
submission. Heavy install/model/GPU/Torch/diffusers work must enter `a100-01`
through Slurm first:

```bash
srun -p a100 -w a100-01 --gres shard:1 --pty /bin/bash -l
```

Use:

```text
/cluster/users/grad/2025/25t8103/project/.venv
```
