# Diagnostics — 2026-07-13

These probes are post-submission and are not paper evidence.

- Shared SD3/FLUX controller shadow: numerical-equivalence check, default off.
- fe_195 dual crowns: SD3 benefited from a tight mask, but the same input
  regressed FLUX; all outputs are diagnostic-only.
- fe_186 raspberry-to-strawberry: removed-object support and an appearance gate
  fixed false muffin masks, but FLUX replacement remained incomplete; all
  outputs are diagnostic-only.
- FLUX two-stage exact-text replacement: did not solve ghosting reliably.

The Slurm launchers are kept in `slurm/`. Results remain in their original
output directories and are not copied or overwritten here.
